<div class="post-index">
  <p>{{#link-to 'post' post}}{{post.title}}{{/link-to}}</p>
  {{user.post.id}}
  {{#if post.editable}}
  <button class="btn btn-danger" {{action 'deletePost'}}>Delete</button>
  {{#link-to 'post.edit' post}}Update{{/link-to}}
  {{/if}}
</div>
{{outlet}}
