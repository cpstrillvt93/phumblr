<nav class="navbar navbar-default">
  <div class="container-fluid">
    {{navbar-header}}

    <div class="collapse navbar-collapse" id="navigation">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
        {{#link-to "users" class='navbar-brand'}}Users{{/link-to}}
        {{#link-to "posts" class='navbar-brand'}}phumblePheed{{/link-to}}
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if isAuthenticated}}
        <li>{{#link-to "change-password"}}Change Password{{/link-to}}</li>
        <li><a href="#" {{action "signOut"}}>Sign Out</a></li>
        {{else}}
        {{#link-to "sign-up" class='navbar-brand'}}Sign Up{{/link-to}}
        {{#link-to "sign-in" class='navbar-brand'}}Sign In{{/link-to}}
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

<h1 class="phumblr-header">Phumblr</h1>

{{#each flashMessages.queue as |flash|}}
  {{flash-message flash=flash}}
{{/each}}

<div class="col-md-8 col-md-offset-2">
  {{outlet}}
</div>
